apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: preslabs
  namespace: database
spec:
  interval: 5m
  chart:
    spec:
      chart: mysql-operator
      #version: ">=4.0.0 <5.0.0"
      sourceRef:
        kind: HelmRepository
        name: preslabs
        namespace: database
      interval: 1m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      remediateLastFailure: false
  values:
    serviceMonitor:
      enabled: false

     
